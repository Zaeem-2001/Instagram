<h3>Update Comment</h3>
<%= form_with  model: [@post, @post.comments.find(@comment.id)], url: post_comment_path do |form| %>

  <p>
    <%= form.text_area :body , class: 'text' , placeholder: 'Add a Comment ...', required: true%>
  </p>

  <p>
    <%= form.submit  %>
  </p>

<% end %>

